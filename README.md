### nodejs app to serve tweets over websocket
* Server
   * nodejs
   * uses twitter sdk
   * uses socket.io

* Client
   * Uses socket.io js for connecting to server
   * Uses jquery for js
   * Uses bootstrap for css
